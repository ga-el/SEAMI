---
// src/pages/index.astro
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>RevisApp - Inicio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>

  <body class="dark-mode">

    <!-- Header -->
    <header class="header">
      <a href="/" class="logo">SEAMI</a>
      <button id="toggle-theme" class="theme-toggle">🌙</button>
    </header>

    <!-- Contenido principal -->
    <main style="padding: 2rem;">
      <section class="glass-container">
        <h2>Bienvenido a SEAMI</h2>
        <p style="text-align: center; margin-bottom: 1.5rem;">
          Tu profe, tus temas, a tu ritmo. Porque nadie explica como el.
        </p>
        <div style="text-align: center;">
          <a href="/login" class="link" style="font-size: 1.1rem;">Iniciar Sesión</a> |
          <a href="/register" class="link" style="margin-left: 0.5rem; font-size: 1.1rem;">Registrarse</a>
        </div>
      </section>
    </main>

    <!-- Script para modo oscuro -->
    <script is:inline>
      const toggleBtn = document.getElementById('toggle-theme');
      const body = document.body;

      // Cargar tema desde localStorage
      const savedTheme = localStorage.getItem('theme');
      if (savedTheme === 'light') {
        body.classList.remove('dark-mode');
        toggleBtn.textContent = '☀️';
      }

      // Cambiar tema y guardar en localStorage
      toggleBtn.addEventListener('click', () => {
        body.classList.toggle('dark-mode');
        const isDarkMode = body.classList.contains('dark-mode');
        if (isDarkMode) {
          toggleBtn.textContent = '🌙';
          localStorage.setItem('theme', 'dark');
        } else {
          toggleBtn.textContent = '☀️';
          localStorage.setItem('theme', 'light');
        }
      });
    </script>

    <style>
      /* Reset básico */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Segoe UI', sans-serif;
      }

      body {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        /* background: linear-gradient(to right bottom, #0f172a, #1e293b);(posible siguiente tema)*/
        background: linear-gradient(to right bottom, #0f172a, #50315c); 
        color: white;
        transition: background 0.4s ease, color 0.4s ease;
      }

      .light-mode {
        background: linear-gradient(to right bottom, #e0e0e0, #ffffff);
        color: #333;
      }

      .light-mode .glass-container {
        background: rgba(255, 255, 255, 0.6);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.3);
      }

      .light-mode input,
      .light-mode select,
      .light-mode button {
        background: rgba(255, 255, 255, 0.9);
        color: #333;
      }

      .light-mode button {
        background-color: #0f172a;
        color: white;
        border: none;
      }

      .light-mode .header {
        background: #ffffffcc;
        color: #0f172a;
        border-bottom: 1px solid #ccc;
      }

      .light-mode .logo {
        color: #0f172a;
      }

      .light-mode .theme-toggle {
        background-color: #0f172a;
        color: white;
      }

      /* === Header === */
      .header {
        position: fixed;
        top: 0;
        width: 100%;
        z-index: 1000;
        display: flex;
        align-items: center;
        justify-content: space-between;
        background-color: rgba(0, 0, 0, 0.6);
        backdrop-filter: blur(10px);
        padding: 1rem 2rem;
        color: white;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
        transition: all 0.3s ease;
      }

      .logo {
        font-size: 1.7rem;
        font-weight: bold;
        color: #8bc34a;
        text-decoration: none;
        cursor: pointer;
        transition: color 0.3s ease;
      }

      .logo:hover {
        color: #557c29;
      }

      .theme-toggle {
        background: transparent;
        border: none;
        font-size: 1.2rem;
        cursor: pointer;
        color: white;
        transition: transform 0.3s ease;
      }

      .theme-toggle:hover {
        transform: rotate(45deg);
      }

      /* === Contenedor Glassmorphic === */
      .glass-container {
        background: rgba(255, 255, 255, 0.05);
        border-radius: 16px;
        padding: 2rem;
        max-width: 600px;
        width: 100%;
        text-align: center;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        animation: fadeInUp 0.8s ease-out;
      }

      h2 {
        font-size: 1.8rem;
        margin-bottom: 1.5rem;
        color: #ffffffcc;
      }

      p {
        text-align: center;
        margin-bottom: 1.5rem;
      }

      a.link {
        color: #8bc34a;
        text-decoration: none;
        transition: color 0.3s ease;
      }

      a.link:hover {
        color: #6a9934;
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @media (max-width: 600px) {
        .glass-container {
          padding: 1.5rem;
          margin: 1rem;
        }

        h2 {
          font-size: 1.5rem;
        }

        a.link {
          font-size: 1rem;
        }
      }
    </style>
  </body>
</html>

